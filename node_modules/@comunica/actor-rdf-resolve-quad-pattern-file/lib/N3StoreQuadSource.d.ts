import { ILazyQuadSource } from "@comunica/bus-rdf-resolve-quad-pattern";
import { AsyncIterator } from "asynciterator";
import * as RDF from "rdf-js";
export declare class N3StoreQuadSource implements ILazyQuadSource {
    protected readonly store: any;
    constructor(store: any);
    match(subject?: RDF.Term | RegExp, predicate?: RDF.Term | RegExp, object?: RDF.Term | RegExp, graph?: RDF.Term | RegExp): RDF.Stream;
    matchLazy?(subject?: RDF.Term | RegExp, predicate?: RDF.Term | RegExp, object?: RDF.Term | RegExp, graph?: RDF.Term | RegExp): AsyncIterator<RDF.Quad> & RDF.Stream;
}
